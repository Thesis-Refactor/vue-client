import Vue from 'vue'
import Vuex from 'vuex'
import AuthStore from '../pages/auth/Store'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    auth: AuthStore
  }
})
